import 'package:flutter/material.dart';

class ManegeScreen extends StatelessWidget {
  const ManegeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Trang chủ quản lý sân")));
  }
}
